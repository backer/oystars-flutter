import 'dart:ui';

const LOGO_BACKGROUND_COLOR = 0xff00a5cf;
// iOS uses different/offset hex values compared to flutter and Android Studio
// for some reason, so include it here just in case we need it
const LOGO_BACKGROUND_COLOR_IOS = 0xff0097C2;
const colorGold = Color(0xffD1B000);
